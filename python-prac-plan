# py‑practice 21天简易执行清单
>目标：脱离习题，学会写解决真实小事的Python脚本；每天30‑60分钟；代码全部存进py‑practice仓库，写完就git提交。
>文件夹结构直接建好：
```
py‑practice/
├─ python程序设计基础
├─ 02‑standard‑lib
├─ 03‑third‑party‑proj
└─ 04‑mini‑projects
```
>每次完成当天任务，执行：
```powershell
git add .
git commit -m "dayX: 当天做的事情简述"
git push github main
```

## 第一周｜夯实基础（熟悉文件、字符串、异常，02‑standard‑lib文件夹）
**Day1**
任务：跑通之前的`count_text.py`；新建test.txt填入中文笔记；脚本统计文件行数、汉字数量。
产出：`count_text.py`
知识点：文件读写 with open、异常捕获try‑except、遍历字符串、判断汉字。

**Day2**
任务：写备忘录小脚本。输入文字，把内容追加保存到`memo.txt`；可以查看全部历史备忘录。
产出：`memo_tool.py`
知识点：文件追加模式，input交互，循环菜单。

**Day3**
任务：练习字符串处理。写脚本，接收一段文本，实现：去除多余空格、提取里面所有数字、简单文本清洗。
产出：`text_clean.py`
知识点：字符串split/strip/replace，遍历筛选。

**Day4**
任务：学习pathlib库，遍历文件夹，打印该文件夹下面所有文件名字。
产出：`list_files.py`
知识点：pathlib路径处理，区分文件/文件夹。

**Day5**
任务：基于Day4代码，做简易批量改名脚本（测试在空文件夹操作，不要乱改系统文件！），给文件加前缀。
产出：`batch_rename.py`
知识点：路径操作，重命名，做好备份再运行。

**Day6**
任务：datetime库练习。写脚本：打印当前时间；计算两个日期相差多少天；输出格式化日志时间字符串。
产出：`time_demo.py`
知识点：datetime，strftime格式化。

**Day7 复盘日**
任务：回顾本周全部脚本；给每个脚本写简短注释；给02‑standard‑lib写一个简易README.md，说明每个py文件干什么。
产出：整理注释、项目说明文档。

## 第二周｜标准库进阶+简单数据处理（02‑standard‑lib）
**Day8**
任务：json库练习。写本地小字典，保存输出到`data.json`；再把json文件读回程序。
产出：`json_io.py`
知识点：json.dump / json.load。

**Day9**
任务：正则re库入门。给定一段杂乱文本，提取里面全部数字；练习匹配手机号。
产出：`re_extract.py`
知识点：正则简单匹配，findall。

**Day10**
任务：整合小脚本：读取一个txt笔记，用正则提取里面所有数字，输出保存到新文件。
产出：`extract_num_from_note.py`

**Day11**
任务：综合小练习：写一个简单运行日志工具，脚本运行自动记录时间、运行事件到log.txt。
产出：`simple_logger.py`

**Day12**
任务：巩固函数。把前面经常复用的代码封装成函数，例如读取文本、提取数字函数。
产出：重构旧脚本，抽取公共函数。

**Day13**
任务：复习前面所有脚本，挑2个不满意的，重构优化，增加异常处理，防止程序轻易崩溃。

**Day14 复盘**
梳理本周；区分：哪些是自带标准库，哪些是以后需要pip装的第三方库；整理笔记。

## 第三周｜第三方库入门，开始做实用小工具（03‑third‑party‑proj）
>注意：所有pip安装都在py‑practice目录终端执行
**Day15**
任务：安装pandas；生成简单csv表格；读取csv，打印表格内容，查看行列。
产出：`pandas_demo.py`
>pip install pandas

**Day16**
任务：pandas实战：造一份模拟成绩csv；统计最高分最低分平均分，筛选不及格数据，输出新csv报告。
产出：`score_stat.py`

**Day17**
任务：openpyxl练习，读写简单Excel xlsx文件，写入几行测试数据。
产出：`excel_demo.py`
>pip install openpyxl

**Day18**
任务：做半完整小工具：读取excel成绩表，统计各科分数，输出简单统计报告。
产出：`excel_score_analysis.py`

**Day19**
任务：requests入门练习，获取公开简单接口数据，转json保存本地。
产出：`fetch_data.py`
>pip install requests

**Day20**
任务：综合整合，挑选前面写的工具，组合成一个小型工具脚本。

**Day21｜产出小项目，迁移到04‑mini‑projects**
任务：完成第一个可以对外展示的完整小项目：**本地文本词频统计工具**
需求：读取本地txt/md，统计词语出现频次，输出统计结果，附带README，写清楚如何运行、依赖库。
产出：完整项目文件夹，可放到简历的小Demo。

---

# 重要提醒
1. 不要追求全部完美，写出来能跑，就算完成。
2. 遇到红色报错，直接把完整报错复制粘贴发给我，不用自己死磕。
3. 不要复制粘贴完就完事：复制一段代码之后，尝试改动参数，观察输出变化，弄懂每一步干什么。
4. 每一个脚本尽量写`if __name__ == "__main__":`入口，养成工程习惯。

如果你某一天时间少，可以只完成半份任务，进度可以灵活拉长。
需要的话，我可以把其中某一天的完整示例代码直接给你。