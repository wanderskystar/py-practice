"""
3章 程序控制结构
一、知识点
1. 程序三大结构：顺序、选择、循环
2. 选择语句
   - 单分支 if 条件:
   - 双分支 if-else
   - 多分支 if-elif-else，自上而下匹配，匹配成功直接结束
   - 支持if嵌套
3. 循环语句
   - while循环：条件不确定场景，先判断条件再执行循环体
   - for循环：搭配range()遍历，range左闭右开 range(0,5) 取0,1,2,3,4
   - range三参数 range(start, stop, step) 起始、终止、步长
4. 跳转语句
   - break：直接跳出**当前一层**循环，循环彻底结束
   - continue：跳过本次循环剩余代码，直接进入下一次条件判断
   - pass：空语句，仅占位，无任何执行逻辑
5. 嵌套循环：外层循环次数 × 内层循环次数 = 总循环执行次数





"""